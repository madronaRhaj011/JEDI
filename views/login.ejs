<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.2.1/flowbite.min.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .harvard-crimson {
            background-color: #A51C30;
        }
        .harvard-text {
            color: #A51C30;
        }
        .auth-container {
            min-height: 100vh;
            background-image: linear-gradient(rgba(165, 28, 48, 0.05), rgba(165, 28, 48, 0.1));
        }
    </style>
</head>
<body class="font-serif">
    <div class="auth-container flex items-center justify-center p-4">
        <div class="grid md:grid-cols-2 gap-8 max-w-6xl w-full bg-white rounded-xl shadow-2xl overflow-hidden">
            <!-- Image Section -->
            <div class="hidden md:block relative">
                <img src="https://images.unsplash.com/photo-1587293852726-70cdb56c2866?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1172&q=80"  alt="Campus Library" class="w-full h-full object-cover">
                <div class="absolute inset-0 bg-gradient-to-t from-black/50 to-transparent flex items-end p-8">
                    <div class="text-white">
                        <h2 class="text-2xl font-bold mb-2">Welcome Back</h2>
                        <p>Access your inventory management dashboard</p>
                    </div>
                </div>
            </div>

            <!-- Login Form -->
            <div class="p-8 md:p-12">
                <div class="mb-8">
                    <h1 class="text-3xl font-bold harvard-text mb-2">Sign In</h1>
                    <p class="text-gray-600">Please sign in to access your account</p>
                </div>

                <!-- Messages Section -->
                <% if(messages.error) { %>
                    <div class="mb-4 p-4 bg-red-100 border border-red-400 text-red-700 rounded">
                        <%= messages.error %>
                    </div>
                <% } %>

                <% if(messages.success) { %>
                    <div class="mb-4 p-4 bg-green-100 border border-green-400 text-green-700 rounded">
                        <%= messages.success %>
                    </div>
                <% } %>

                <form action="/login" method="post" class="space-y-6">
                    <div>
                        <label for="username" class="block text-sm font-medium text-gray-700 mb-2">Username</label>
                        <input 
                            type="text" 
                            id="username" 
                            name="username" 
                            required 
                            class="w-full px-4 py-2 border rounded-lg focus:ring-2 focus:ring-red-500 focus:border-transparent" 
                            placeholder="Enter you username"
                        >
                    </div>

                    <div>
                        <label for="password" class="block text-sm font-medium text-gray-700 mb-2">Password</label>
                        <input 
                            type="password" 
                            id="password" 
                            name="password" 
                            required 
                            class="w-full px-4 py-2 border rounded-lg focus:ring-2 focus:ring-red-500 focus:border-transparent" 
                            placeholder="Enter your password"
                        >
                    </div>

                    <button 
                        type="submit" 
                        class="w-full harvard-crimson text-white py-2 rounded-lg hover:bg-red-900 transition duration-300"
                    >
                        Submit
                    </button>

                    <p class="text-center text-gray-600 text-sm">
                        Don't have an account? 
                        <a href="/register" class="harvard-text hover:text-red-800">Register here</a>
                    </p>
                </form>
            </div>
        </div>
    </div>
</body>
</html>